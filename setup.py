from setuptools import setup, find_packages

setup(
    name='matrix_analyzer',
    version='0.1',
    description='A simple Python library for matrix\'s properties analysing.',
    author='Lisa Bugir and Mykola Balyk',
    packages=find_packages(),
)
